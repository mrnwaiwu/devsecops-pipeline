# Bandit SAST configuration
# Docs: https://bandit.readthedocs.io/en/latest/config.html

skips: []

# Exclude test files and virtual envs from scanning
exclude_dirs:
  - tests
  - .venv
  - venv

# Flag medium and above severity
level: MEDIUM
confidence: MEDIUM
